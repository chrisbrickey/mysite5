<%= form_for(project) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title, cols: 60 %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, rows: 10, cols: 60 %>
  </div>

  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_area :image_url, cols: 30 %>
  </div>

  <div class="field">
    <%= f.label :hyperlink %>
    <%= f.text_area :hyperlink, cols: 30 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
